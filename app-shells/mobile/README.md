# Mobile Application Shell

- Provides a config-driven UI shell for Core OS (Expo/React Native).
- Wires to core/control-plane contracts (interfaces only).
- No business, domain, or workflow logic.
- All UI is placeholder and config-driven.
